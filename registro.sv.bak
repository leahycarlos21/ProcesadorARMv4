module registro #(parameter size = 8)(input logic clk, rst, enable,
													input logic [size-1:0] in,
													output logic [size-1:0] out);
always_ff(posedge clk, posedge rst)
	if(rst) 
		out<=0;
	else(enable)
		out<=in;
endmodule 